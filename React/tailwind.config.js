/** @type {import('tailwindcss').Config} */
export default {
  content: ['./src/**/*.{js,jsx,ts,tsx}'], // Chỉ định đường dẫn tới các tệp sử dụng class Tailwind
  theme: {
    extend: {}, // Dùng để mở rộng theme
  },
  plugins: [], // Thêm plugin tại đây nếu cần
};
